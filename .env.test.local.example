# Copy to .env.test.local (gitignored) and fill in the anon key.
# Run `supabase start` (or `supabase status -o env`) and copy its printed
# API URL / anon key here. This points the integration test suite at the
# LOCAL Docker Supabase stack — never the hosted project.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=###
